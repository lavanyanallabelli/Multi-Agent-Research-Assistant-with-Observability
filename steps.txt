🟦 PHASE 1 — Project Setup

Step 1.1 — Install Python, VS Code, extensions
Step 1.2 — Create virtual environment (Python's node_modules)
Step 1.3 — Project folder structure
Step 1.4 — Install all dependencies
Step 1.5 — .env setup (API keys)
Step 1.6 — Run your first Python file


🟦 PHASE 2 — Data Layer (Real Market Data)

Step 2.1 — Connect to yfinance (free real stock data)
Step 2.2 — Fetch OHLCV data (Open, High, Low, Close, Volume)
Step 2.3 — Calculate technical indicators (RSI, MACD, Bollinger Bands, EMA)
Step 2.4 — Fetch real financial news via NewsAPI / Tavily
Step 2.5 — Build a MarketDataTool class (reusable across agents)


🟦 PHASE 3 — Tool/Function Calling Layer

Step 3.1 — What is tool calling & why it matters
Step 3.2 — Define typed tool schemas (like TypeScript interfaces)
Step 3.3 — Build these tools:

get_stock_data tool
get_technical_indicators tool
get_market_news tool
calculate_risk_reward tool


Step 3.4 — Parallel tool execution with asyncio.gather() (like Promise.all)
Step 3.5 — Structured error returns per tool


🟦 PHASE 4 — Build Each Agent

Step 4.1 — Base Agent class (shared logic)
Step 4.2 — Orchestrator Agent — parses query, builds task plan, routes agents
Step 4.3 — Research Agent — calls tools, gathers raw stock + news data
Step 4.4 — Analyst Agent — interprets data, finds patterns, calculates R/R
Step 4.5 — Writer Agent — generates structured trading report (with streaming)
Step 4.6 — Critic Agent — scores report quality, triggers revision if needed
Step 4.7 — Critic revision loop with circuit breaker (max 3 revisions)


🟦 PHASE 5 — Shared Memory & Audit Log

Step 5.1 — What is shared memory in multi-agent systems
Step 5.2 — Build AgentMemory class — stores context across all agent hops
Step 5.3 — Full audit log — every agent decision recorded with timestamp
Step 5.4 — Prevent duplicate work (if research done, skip re-fetch)
Step 5.5 — Post-hoc debugging — replay any agent's decision


🟦 PHASE 6 — Observability Layer

Step 6.1 — What is observability & why it matters in production
Step 6.2 — Distributed tracing — trace one full request across all agents
Step 6.3 — Per-agent token tracking & cost calculation
Step 6.4 — Success/failure rate tracking per agent
Step 6.5 — Build a simple terminal dashboard (live stats)
Step 6.6 — Request replay for debugging failed runs


🟦 PHASE 7 — Streaming Support

Step 7.1 — What is streaming & why it matters (400ms first token!)
Step 7.2 — Add streaming to Writer Agent output
Step 7.3 — Stream to terminal (we'll later connect to UI)


🟦 PHASE 8 — Production Hardening

Step 8.1 — Exponential backoff retry (transient vs permanent errors)
Step 8.2 — Graceful degradation (if one agent fails, system continues)
Step 8.3 — Context window management (auto summarize long conversations)
Step 8.4 — Input validation with Pydantic (Python's Zod equivalent)
Step 8.5 — Environment configs for dev vs prod


🟦 PHASE 9 — Putting It All Together

Step 9.1 — Wire all agents into one pipeline
Step 9.2 — End-to-end test: "Analyze TSLA for swing trade opportunity"
Step 9.3 — Sample outputs & report review
Step 9.4 — Connect to your Gateway (Project 3) for cost control
Step 9.5 — Final resume bullet polish with real metrics


🟦 PHASE 10 — Optional: Simple FastAPI Web Interface

Step 10.1 — Expose the agent pipeline as a REST API
Step 10.2 — Add a minimal frontend to submit queries
Step 10.3 — Show streaming output in the browser